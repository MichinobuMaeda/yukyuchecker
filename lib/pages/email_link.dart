import 'package:flutter/material.dart';

class EmailLinkPage extends StatelessWidget {
  const EmailLinkPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('メールリンクでサインインします。'));
  }
}
